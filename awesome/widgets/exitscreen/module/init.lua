return {
  colors  = require(... .. '.colors'),
  battery = require(... .. '.battery'),
  time    = require(... .. '.time'),
  topbar  = require(... .. '.topbar'),
  music   = require(... .. '.music')
}
