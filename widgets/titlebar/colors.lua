local colorscheme = require('theme.colorscheme')

local colors = {}

colors = {
    titlebar_fg_normal = colorscheme.bg_light,
    red          = colorscheme.red,
    blue         = colorscheme.blue,
    magenta      = colorscheme.magenta
}

return colors
