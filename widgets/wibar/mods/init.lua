return {
    me        = require(... .. '.me'),
    tags      = require(... .. '.tags'),
    tasks     = require(... .. '.tasks'),
    colors    = require(... .. '.colors'),
    layout    = require(... .. '.layout'),
    systray   = require(... .. '.systray'),
    music     = require(... .. '.music'),
    bluetooth = require(... .. '.bluetooth'),
    wifi      = require(... .. '.wifi'),
    battery   = require(... .. '.battery'),
    time  = require(... .. '.time'),
    theme_switch = require(... .. '.theme'),
}